from setuptools import find_packages, setup

setup(name='aci',
      version='0.0.4',
      description='Mock library developed during Python training',
      url='http://github.com/cisco/aci-examples',
      packages=find_packages(),
      include_package_data=True)
